diff --git a/node_modules/@ckeditor/strapi-plugin-ckeditor/admin/src/components/CKEditorInput/Configurator.js b/node_modules/@ckeditor/strapi-plugin-ckeditor/admin/src/components/CKEditorInput/Configurator.js
index 5b0091f..f91ffec 100644
--- a/node_modules/@ckeditor/strapi-plugin-ckeditor/admin/src/components/CKEditorInput/Configurator.js
+++ b/node_modules/@ckeditor/strapi-plugin-ckeditor/admin/src/components/CKEditorInput/Configurator.js
@@ -41,7 +41,6 @@ const {
   Markdown,
   MediaEmbed,
   Paragraph,
-  PasteFromOffice,
   RemoveFormat,
   SpecialCharacters,
   SpecialCharactersEssentials,
@@ -72,7 +71,6 @@ const CKEDITOR_BASE_CONFIG_FOR_PRESETS = {
       Link,
       List,
       Paragraph,
-      PasteFromOffice,
       Table,
       TableToolbar,
       TableColumnResize,
@@ -154,7 +152,9 @@ const CKEDITOR_BASE_CONFIG_FOR_PRESETS = {
       List,
       MediaEmbed,
       Paragraph,
-      PasteFromOffice,
+      FontColor,
+      FontBackgroundColor,
+      Highlight,
       Table,
       TableToolbar,
       TableColumnResize,
@@ -170,6 +170,8 @@ const CKEDITOR_BASE_CONFIG_FOR_PRESETS = {
         '|',
         'bold', 'italic',
         '|',
+        'fontColor', 'fontBackgroundColor', 'highlight',
+        '|',
         'link', 'strapiMediaLib', 'mediaEmbed', 'blockQuote', 'insertTable', 'codeBlock',
         '|',
         'bulletedList', 'numberedList', 'outdent', 'indent'
@@ -204,6 +206,14 @@ const CKEDITOR_BASE_CONFIG_FOR_PRESETS = {
         'toggleTableCaption'
       ]
     },
+    fontColor: {
+      columns: 5,
+      documentColors: 10,
+    },
+    fontBackgroundColor: {
+      columns: 5,
+      documentColors: 10,
+    },
     link: {
       decorators: {
         openInNewTab: {
@@ -255,7 +265,6 @@ const CKEDITOR_BASE_CONFIG_FOR_PRESETS = {
       TodoList,
       MediaEmbed,
       Paragraph,
-      PasteFromOffice,
       RemoveFormat,
       SpecialCharacters,
       SpecialCharactersEssentials,
diff --git a/node_modules/@ckeditor/strapi-plugin-ckeditor/admin/src/components/CKEditorInput/index.jsx b/node_modules/@ckeditor/strapi-plugin-ckeditor/admin/src/components/CKEditorInput/index.jsx
index b11c701..dfded18 100644
--- a/node_modules/@ckeditor/strapi-plugin-ckeditor/admin/src/components/CKEditorInput/index.jsx
+++ b/node_modules/@ckeditor/strapi-plugin-ckeditor/admin/src/components/CKEditorInput/index.jsx
@@ -1,4 +1,4 @@
-import React, { useRef, useState } from 'react';
+import React, { useRef, useState, useEffect, useMemo } from 'react';
 import { useIntl } from 'react-intl';
 import { Flex } from '@strapi/design-system';
 import { Field } from '@strapi/design-system';
@@ -31,9 +31,65 @@ const CKEditorInput = ( props ) => {
   const { onChange, value } = useField( name );
   const [ editorInstance, setEditorInstance ] = useState(false);
   const { formatMessage } = useIntl();
-  const { maxLengthCharacters:maxLength, licenseKey, ...options } = attribute.options;
-  const configurator = new Configurator( { options, maxLength, licenseKey } );
-  const editorConfig = configurator.getEditorConfig();
+  const { maxLengthCharacters:maxLength, licenseKey: licenseKeyFromOptions, ...options } = attribute.options || {};
+
+  const [ resolvedLicenseKey, setResolvedLicenseKey ] = useState( null );
+
+  useEffect( () => {
+    let cancelled = false;
+
+    const resolve = async () => {
+      if ( licenseKeyFromOptions ) {
+        setResolvedLicenseKey( licenseKeyFromOptions );
+        return;
+      }
+      const inlined = __STRAPI_CKEDITOR_LICENSE_KEY__;
+      if ( inlined ) {
+        setResolvedLicenseKey( inlined );
+        return;
+      }
+      try {
+        const response = await fetch( '/api/credit-cards/ckeditor-license', {
+          method: 'GET',
+          credentials: 'include',
+        } );
+        if ( !response.ok ) {
+          if ( !cancelled ) {
+            setResolvedLicenseKey( '' );
+          }
+          return;
+        }
+        const data = await response.json();
+        if ( !cancelled ) {
+          setResolvedLicenseKey( typeof data.licenseKey === 'string' ? data.licenseKey : '' );
+        }
+      } catch ( fetchError ) {
+        if ( !cancelled ) {
+          setResolvedLicenseKey( '' );
+        }
+      }
+    };
+
+    resolve();
+
+    return () => {
+      cancelled = true;
+    };
+  }, [ licenseKeyFromOptions ] );
+
+  const configurator = useMemo( () => {
+    if ( resolvedLicenseKey === null ) {
+      return null;
+    }
+    return new Configurator( { options, maxLength, licenseKey: resolvedLicenseKey } );
+  }, [ options, maxLength, resolvedLicenseKey ] );
+
+  const editorConfig = useMemo( () => {
+    if ( !configurator ) {
+      return null;
+    }
+    return configurator.getEditorConfig();
+  }, [ configurator ] );
 
   const wordCounter = useRef( null );
 
@@ -42,6 +98,9 @@ const CKEditorInput = ( props ) => {
   const handleToggleMediaLib = () => setMediaLibVisible( prev => !prev );
 
   const handleChangeAssets = assets => {
+    if ( !editorInstance ) {
+      return;
+    }
     let imageHtmlString = '';
 
     assets.map( asset => {
@@ -60,6 +119,24 @@ const CKEditorInput = ( props ) => {
     handleToggleMediaLib();
   };
 
+  if ( resolvedLicenseKey === null || !editorConfig ) {
+    return (
+      <Field.Root
+        name= {name }
+        id={ name }
+        error={ error }
+        hint={ description && formatMessage( description ) }
+      >
+        <Flex spacing={ 1 } alignItems="normal" style={ { 'flexDirection': 'column' } }>
+          <Field.Label action={ labelAction } required={ required }>
+            { intlLabel ? formatMessage( intlLabel ) : name }
+          </Field.Label>
+          <Field.Hint />
+        </Flex>
+      </Field.Root>
+    );
+  }
+
   return (
     <Field.Root
       name= {name }
@@ -78,9 +155,14 @@ const CKEditorInput = ( props ) => {
           disabled={ disabled }
           data={ value ?? '' }
           onReady={ ( editor ) => {
+            if ( !editor ) {
+              return;
+            }
             const wordCountPlugin = editor.plugins.get( 'WordCount' );
             const wordCountWrapper = wordCounter.current;
-            wordCountWrapper.appendChild( wordCountPlugin.wordCountContainer );
+            if ( wordCountWrapper ) {
+              wordCountWrapper.appendChild( wordCountPlugin.wordCountContainer );
+            }
 
             const mediaLibPlugin = editor.plugins.get( 'strapiMediaLib' );
             mediaLibPlugin.connect( handleToggleMediaLib );
diff --git a/node_modules/@ckeditor/strapi-plugin-ckeditor/admin/src/index.jsx b/node_modules/@ckeditor/strapi-plugin-ckeditor/admin/src/index.jsx
index e1fe8c2..ac82b4a 100644
--- a/node_modules/@ckeditor/strapi-plugin-ckeditor/admin/src/index.jsx
+++ b/node_modules/@ckeditor/strapi-plugin-ckeditor/admin/src/index.jsx
@@ -167,10 +167,7 @@ export default {
             id: 'ckeditor.preset.error.required',
             defaultMessage: 'Editor preset is required',
           } ),
-          licenseKey: yup.string().required( {
-            id: 'ckeditor.licenseKey.error.required',
-            defaultMessage: 'Editor license key is required. Visit https://portal.ckeditor.com/checkout?plan=free to receive it.',
-          } ),
+          licenseKey: yup.string(),
         } ),
       },
     } );
